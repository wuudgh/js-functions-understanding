//20 will be logged because the values of the arguments given before the function was invoked.
//If the funtion was invoked before the values where given it will log nudefined.

function myFunction(num1, num2) {
  console.log(num3);
}

const num1 = 10;
const num2 = 1;
const num3 = 20;

myFunction(num3, num1);
